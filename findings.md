# 调研记录

## 当前实现

- 胶囊内容已包含状态点、项目名、任务标题、活动文案、摘要预览和菜单入口。
- `renderCapsule` 会在渲染时设置 `data-kind`、`data-runtime-state` 并同步文本。
- 胶囊模式通过 `state.mode === 'capsule'` 控制，窗口尺寸由 Tauri `set_window_mode` 控制。

## 设计决策

- 采用“顶部悬浮黑色胶囊 + 状态光晕 + hover/focus 展开”的方向。
- 复用现有内容和事件，不复制一套动态岛状态逻辑。
- CSS 优先，减少 JS 状态新增；展开状态仅作为胶囊自身的交互样式。

## 已实现

- 默认岛面高度 `54px`，展开态高度 `128px`。
- 鼠标、键盘焦点和菜单均可保持展开。
- 展开态复用原 `capPreview` 数据，紧凑态保留状态、项目、标题、进度和未读标记。
- `prefers-reduced-motion` 下禁用过渡和工作态呼吸动画。

## FocuSD 初步对照

- FocuSD 是 Windows 优先的 Tauri 2 + React 19 灵动岛效率工具。
- 产品边界是待办、笔记、剪贴板、媒体控制与 Codex 状态灯，不是完整 Agent 会话聚合器。
- README 明确其窗口为透明、无边框、始终置顶、屏幕顶部停靠，并支持折叠、边缘收起和托盘隐藏。
- 当前仓库未声明开源许可证，不能默认复制代码；只可把公开实现作为设计与工程参考。

## FocuSD 实现结论

- 折叠岛面宽度约 `240–320px`、高度 `58px`；展开态约 `560px`，位于固定 `820×460` 透明舞台窗口内。
- Windows 后端约每 `12ms` 做一次真实圆角岛面命中测试，并对岛外区域启用点击穿透。
- 该方案依赖 Windows 原生 API，不适合直接移植到本项目的 macOS/Windows 双平台窗口层。
- 本项目岛面宽度固定为 `360px`，无需保留大透明舞台。采用 `62px` 收起窗口与 `136px` 展开窗口两档原生尺寸，可以直接消除透明点击死区，复杂度和长期维护成本都更低。
- FocuSD 启动隐藏、几何就绪后显示，失焦/Escape 收起等细节值得后续按实际体验增量引入；当前不复制其多功能面板与高频轮询架构。
